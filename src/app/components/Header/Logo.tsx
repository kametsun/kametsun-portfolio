import { type ReactElement } from 'react'

export const Logo = (): ReactElement => {
  return (
        <div className={'flex items-center ml-3'}>
            <h2 className={'text-xl font-bold'}>kametsun</h2>
        </div>
  )
}
